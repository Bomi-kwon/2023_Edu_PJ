<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreaIT.project.repository.ArticleRepository">

	<select id="getArticles" resultType="Article">
		SELECT * FROM article
		WHERE boardId = #{boardId}
		ORDER BY id DESC
	</select>
	
	<insert id="doWrite">
		INSERT INTO article
		SET regDate = NOW(),
		updateDate = NOW(),
		memberId = 2,
		boardId = 2,
		title = #{title},
		`body` = #{body};
	</insert>
	
	<select id="getLastId" resultType="Int">
		SELECT LAST_INSERT_ID();
	</select>
	
	<select id="getArticleById" resultType="Article">
		SELECT * FROM article
		WHERE id = #{id}
	</select>
	
	<delete id="doHomeworkDelete">
		DELETE FROM article
		WHERE id = #{id}
	</delete>
	
	<update id="doHomeworkModify">
		UPDATE article
		SET updateDate = NOW(),
		title = #{title},
		`body` = #{body}
		WHERE id = #{id}
	</update>


</mapper>